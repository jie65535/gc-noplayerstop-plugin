plugins {
    id 'java'
}

group 'com.github.jie65535.noplayerstop'
version '0.1.0'

repositories {
    mavenCentral()
}

dependencies {
    implementation fileTree(dir: 'lib', include: ['*.jar'])
}

jar {
    jar.baseName = 'NoPlayerStop'

    destinationDir = file(".")
}